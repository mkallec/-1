# Spider Overlay — Netlify 部署配置
# 放在 GitHub 仓库根目录

[build]
  # 告诉 Netlify 去哪个文件夹找文件
  base = "spider-overlay-v2/netlify/"
  # 无需构建
  command = ""
  # 发布 base 目录下的所有内容
  publish = "."

# 这样部署后：
#   https://你的域名.netlify.app/spider-overlay.js
#   https://你的域名.netlify.app/config.json
